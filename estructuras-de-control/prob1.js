let tipoEntrada = "VIP"; 

if (tipoEntrada === "VIP") {
    console.log(`Tienes acceso a la zona VIP con tu entrada ${tipoEntrada}`);
} else {
    console.log(`Tienes una entrada ${tipoEntrada}, solo puedes acceder a la zona normal.`);
}